<% order.products.each do |product| %>
  <tr>
    <td><%= link_to order.id, order_path(order) %></td>
    <td><%= product.name %></td>
    <td><%= product.price %></td>
    <td><%= order.customer.name %></td>
  </tr>
<% end %>
